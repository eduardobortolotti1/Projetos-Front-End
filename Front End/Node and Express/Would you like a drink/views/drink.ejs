<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Would you like a drink?</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Hind+Siliguri:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="./styles/common.css" />
    <link rel="stylesheet" href="./styles/navbar.css" />
    <link rel="stylesheet" href="./styles/button-menu.css" />
    <link rel="stylesheet" href="./styles/drink-page.css" />
    <link rel="stylesheet" href="./styles/footer.css" />
    <link rel="stylesheet" href="./styles/item-list.css" />
  </head>

  <body>
    <main>
      <%- include('partials/navbar.ejs') %>

      <div class="main-content container px-5">
        <div class="drink-section">
          <div class="row">
            <div class="col-md-6 col-sm-12">
              <h2 id="drink-title"><%= drinkInfo.strDrink %></h2>
              <img
                src="<%= drinkInfo.strDrinkThumb %>"
                alt="drink image"
                id="drink-image"
              />
              <p id="image-credit">
                Credit goes here: <a href="https://www.google.com">google</a>
              </p>
            </div>
            <div class="col-md-6 col-sm-12">
              <h2>Instructions:</h2>
              <p id="drink-instructions">
                <%= drinkInfo.strInstructions %>
              </p>
            </div>
          </div>

          <h2>Ingredients:</h2>
          <div class="container p-0">
            <div class="row item-menu g-0">
              <% for (let key in drinkInfo) { %>
                <% if (key.startsWith("strIngredient") && drinkInfo[key] !== null ) {%>
                  <% let number = key.match(/\d+$/); %>
                  <div class="item">
                  <a href="<%= "/ingredient?ingredientName="+ drinkInfo[key] %>"
                    ><img
                      src="<%= "https://www.thecocktaildb.com/images/ingredients/" + drinkInfo[key] + "-Small.png" %>"
                      alt="ingredient"
                  /></a>
                  <p id="item-name"><%= drinkInfo[`strMeasure${number}`] %> of <%= drinkInfo[key] %></p>
                  </div>
                <% } %>
              <% } %>
          </div>
        </div>

        <%- include('partials/button-menu.ejs') %>

        <%- include('partials/footer.ejs')  %>
      </div>
    </main>
  </body>
</html>
